## 0. Fix Record

- FIX ID: fix-005
- DATE: 2026.03.14
- STATUS: Deployed
- RELATED VERSION: 1.0.5
- IMPLEMENTATION BRANCH: main

## 1. Summary

Moved the changelog into the add-on subfolder and updated the release rules to require the correct path.

## 2. Issue Description

The changelog requirement had been documented against a root-level `CHANGELOG.md`, but the desired location is inside the add-on folder. That mismatch meant the documentation rules and file placement did not align with the intended repository structure.

## 3. Root Cause

The original changelog requirement was added before the final preferred file location was clarified, so the governance text and file placement were both anchored to the repository root.

## 4. Resolution

Moved the changelog to `ev_charge_control/CHANGELOG.md` and updated `CONTEXT.md` and `VERSIONING.md` so future version changes must maintain that specific file path.

## 5. Scope and Impact

This fix affects documentation structure and release governance only.

Impact:
- The changelog now lives with the deployable add-on content.
- The documentation rules now point to the correct maintained file.
- Runtime behavior is unchanged.

Residual risk:
- Any local tooling or habits still pointing at a root `CHANGELOG.md` must be updated.

## 6. Validation

Validated by:
- confirming the changelog exists at `ev_charge_control/CHANGELOG.md`;
- reviewing the updated references in `CONTEXT.md` and `VERSIONING.md`.

## 7. Deployment Notes

This fix is deployed as version `1.0.5` and is intended to be included in the tagged release contents.

No migration or Home Assistant configuration changes are required.

## 8. References

- Files: `CONTEXT.md`, `VERSIONING.md`, `ev_charge_control/CHANGELOG.md`
- Repository: `https://github.com/cajo-dk/ev-charge-control`
