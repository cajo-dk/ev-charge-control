## 0. Fix Record

- FIX ID: fix-007
- DATE: 2026.03.14
- STATUS: Deployed
- RELATED VERSION: 1.0.7
- IMPLEMENTATION BRANCH: main

## 1. Summary

Released a corrective fix version so the changelog entry and add-on version are aligned in the tagged release.

## 2. Issue Description

Version `1.0.6` was tagged while the changelog content visible to Home Assistant still lagged behind the released version. That meant the displayed changelog did not accurately represent the version being loaded.

## 3. Root Cause

Although the release rules had been clarified, the actual `1.0.6` tagged commit still contained stale changelog content from the previous version.

## 4. Resolution

Bumped the add-on to version `1.0.7` and updated `ev_charge_control/CHANGELOG.md` so the newest changelog entry now matches the version being released.

## 5. Scope and Impact

This fix affects release metadata and changelog accuracy only.

Impact:
- The Home Assistant add-on version and visible changelog are aligned again.
- The tagged release contents now reflect the correct latest changelog entry.
- Runtime behavior is unchanged.

Residual risk:
- The previously published `1.0.6` tag still exists with stale changelog content.

## 6. Validation

Validated by:
- confirming `ev_charge_control/config.yaml` is set to `1.0.7`;
- confirming the newest heading in `ev_charge_control/CHANGELOG.md` is `1.0.7`;
- running the automated test suite successfully with `17 passed`.

## 7. Deployment Notes

This fix is deployed as version `1.0.7` and is intended to supersede the changelog inconsistency present in `1.0.6`.

No migration or Home Assistant configuration changes are required.

## 8. References

- Files: `ev_charge_control/CHANGELOG.md`, `ev_charge_control/config.yaml`
- Previous related fix: `doc/fixes/fix-006.md`
- Repository: `https://github.com/cajo-dk/ev-charge-control`
